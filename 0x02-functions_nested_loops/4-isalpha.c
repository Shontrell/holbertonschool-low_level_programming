#include "holberton.h"
/**
 * _isalpha - Entry point
 *
 * @c: int passed as argument to the function
 *
 * Return: 0
 */
int _isalpha(int c)
{
	if(c >= 65 && c <= 122)
	{
		return (1)
			}
	else
	{ return (0)
			}
}
